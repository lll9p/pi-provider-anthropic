{
    "name": "pi-extension-provider-anthropic",
    "private": true,
    "version": "1.8.1",
    "type": "module",
    "scripts": {
        "build": "tsc --noEmit",
        "check": "tsc --noEmit && prettier --check .",
        "format": "prettier --write ."
    },
    "pi": {
        "extensions": [
            "./index.ts"
        ]
    },
    "dependencies": {
        "@anthropic-ai/sdk": "^0.70.0",
        "@mariozechner/pi-ai": "^0.57.1",
        "@mariozechner/pi-coding-agent": "^0.57.1"
    },
    "devDependencies": {
        "prettier": "^3.0.0",
        "typescript": "^5.0.0"
    }
}
